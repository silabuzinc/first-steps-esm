export function eval_number(num) {
    if (num % 2) {
        console.log(`${num} es un número impar`);
    }
    else {
        console.log(`${num} es un número par`);
    }
}

export function saludo() {
    console.log("Hola 👋")
}